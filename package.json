{
  "name": "mahindra-website",
  "private": true,
  "version": "1.0.0",
  "description": "Mahindra Nepal website monorepo: /frontend (Vite + React) and /backend (Express + Nodemailer).",
  "scripts": {
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "server": "npm --prefix backend start",
    "server:dev": "npm --prefix backend run dev",
    "dev:all": "concurrently -k -n web,api -c blue,green \"npm:dev\" \"npm:server:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
